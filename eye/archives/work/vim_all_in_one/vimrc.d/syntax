if has("syntax")
  syntax on
endif

" =============== Syntastic ========================

let g:syntastic_auto_loc_list=1
"let g:syntastic_loc_list_height=2
let g:syntastic_mode_map = { 'mode': 'passive', 'active_filetypes': ['sh','ruby']}
"let g:syntastic_mode_map = { 'mode': 'passive', 'active_filetypes': ['sh','ruby','java']}
set statusline+=%#warningmsg#
set statusline+=%{SyntasticStatuslineFlag()}
set statusline+=%*
map <F7> :SyntasticCheck<CR>

" vim: ft=vim
