check process activemq
   with pidfile "/var/run/activemq.pid"
   start program = "/etc/init.d/activemq restart"
   stop program = "/etc/init.d/activemq stop"
   alert <%= monit_conf_alert_email %>
   if 3 restarts within 4 cycles then timeout
   if cpu usage > 95% for 3 cycles then restart
   if cpu usage > 60% for 3 cycles then alert
   if failed host <%= active_mq_monit_addr %> port 61613 type TCP send "CONNECT\0x0Alogin: monit\0x0Apasscode: <%= active_mq_monit_password -%>\0x0A\0x0A\0x00" expect "CONNECTED\0x0Asession:ID:" for 3 cycles then restart
