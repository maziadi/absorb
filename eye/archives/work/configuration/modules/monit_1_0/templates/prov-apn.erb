check process prov-apn
   with pidfile "/var/run/prov-apn.pid"
   start program = "/etc/init.d/prov-apn start"
   stop program = "/etc/init.d/prov-apn stop"
   alert <%= monit_conf_alert_email %>
   if 3 restarts within 4 cycles then timeout
   if cpu usage > 60% for 3 cycles then alert
   if cpu usage > 95% for 3 cycles then restart
