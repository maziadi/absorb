configuration:
  name             : prov_as
  timeout          : 2
  ON_MESSAGE_DELAY : 0
  loglevel         : info
  logdestination   : syslog,file
  stomp_urls:
    stomp1:
      username     : "<%= prov_as_user -%>"
      password     : "<%= prov_as_password -%>"
      host         : <%= prov_activemq_host %>
      port         : 61613
      ssl          : false

  subscribes:
    "/queue/provisioning" :
      selector    : node_name='<%= hostname -%>-as'
  format          : application/json # Default is text (xml => text/xml | json => application/json)

  daemon:
    username  : "<%= prov_as_db_user -%>"
    password  : "<%= prov_as_db_password -%>"
    host      : "localhost"
    database  : "<%= prov_as_db_name -%>"
