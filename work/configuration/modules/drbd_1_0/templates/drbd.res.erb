resource <%= name %> {
  device <%= device %>;
  disk /dev/<%= vg %>/<%= name %>;
  meta-disk internal;
  
  on <%= host1 %> {
    address <%= ip1 %>:<%= port %>;
  }
  
  on <%= host2 %> {
    address <%= ip2 %>:<%= port %>;
  }
}
