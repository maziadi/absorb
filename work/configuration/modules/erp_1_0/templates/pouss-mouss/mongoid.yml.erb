<%= environment %>:
  sessions:
    default:
      database: pouss_mouss_<%= environment %>
      hosts:
        - <%= db_host %>:27017
      options:
        consistency: :strong
  options:
    raise_not_found_error: false
