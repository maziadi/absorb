check process cas 
  with pidfile "/srv/cas/unicorn.pid"
  start program = "/bin/su - cas -c 'unicorn -E production -c /srv/cas/config/unicorn.rb -D'" 
  stop program = "/bin/su - cas -c 'killall -QUIT unicorn'"
