check device activemq_store with path /var/lib/activemq-data
    alert <%= monit_conf_alert_email %> 
    if space usage > 10% for 5 times within 15 cycles then alert
    if space usage > 50% for 5 times within 15 cycles then alert
    if space usage > 80% for 5 times within 15 cycles then alert
    if inode usage > 90% for 5 times within 15 cycles then alert 
