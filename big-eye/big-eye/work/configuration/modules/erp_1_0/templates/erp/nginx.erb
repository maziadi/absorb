server {
  listen <%= erp_ip %>:80;

  root /;
  index  index.html index.htm;
  proxy_read_timeout 600;
  
  location /data/files {
       root /;
       #internal;
  }

  location /gestion {
    alias /srv/erp/public/;
  }
  location /documents {
    alias /data/files/;
  }
}
